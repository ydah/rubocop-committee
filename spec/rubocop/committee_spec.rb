# frozen_string_literal: true

RSpec.describe Rubocop::Committee do
  it "has a version number" do
    expect(Rubocop::Committee::VERSION).not_to be_nil
  end

  it "does something useful" do
    expect(false).to be(true)
  end
end
